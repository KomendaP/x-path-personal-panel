module.exports = function (ngModule) {
  describe('HomeSection directive test', function () {
    "use strict";
    
    beforeEach(window.module(ngModule.name));
    
    it('should pass test', function () {
      expect(false).to.be.true;
    });
  });
};